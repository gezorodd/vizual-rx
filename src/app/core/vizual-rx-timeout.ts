import {Observable, ObservableInput, ObservedValueOf, OperatorFunction, timeout, TimeoutConfig} from "rxjs";
import {vizualRxScheduler} from "./vizual-rx-scheduler";




// export const vizualRxTimeout = timeout;
